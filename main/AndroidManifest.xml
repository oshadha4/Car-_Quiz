<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mobilecw2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileCW2">
        <activity android:name=".Favourite"></activity>
        <activity android:name=".Rating1" />
        <activity android:name=".Ratings" />
        <activity android:name=".searchfilm" />
        <activity android:name=".editFilm" />
        <activity android:name=".Edit" />
        <activity android:name=".display" />
        <activity android:name=".InsertData" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>